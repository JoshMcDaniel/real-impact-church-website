import { createTheme } from '@mui/material';

/**
 * The custom application theme.
 */
export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#2f2f2f',
    },
    secondary: {
      main: '#f50057',
    },
  },
});
